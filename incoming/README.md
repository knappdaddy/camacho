Put real photographs here, named after the slot they fill, then run:

    python3 tools/import-photos.py --apply

Slot names and what each one needs are in ../PHOTO-BRIEF.md
(`python3 tools/import-photos.py --list` prints them too).

Anything you don't supply keeps its illustrated placeholder, so you can bring
photos in a few at a time.
